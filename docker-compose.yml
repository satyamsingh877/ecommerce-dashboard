version: '3.8'

services:
  ecommerce-dashboard:
    build: .
    container_name: ecommerce-dashboard
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html:ro
    networks:
      - ecommerce-net

networks:
  ecommerce-net:
    driver: bridge
